# mots-dits-website

Static landing page for MotsDits.

## Cloudflare Pages

- Build command: leave empty
- Build output directory: `/`
- Production branch: `main`

## Local preview

Open `index.html` directly in a browser, or run:

```bash
python3 -m http.server 8788
```

Then visit `http://localhost:8788`.

## Launch TODO

- Buy and connect `motsdits.ca` in Cloudflare Pages.
- Replace download placeholders with GitHub release links.
- Replace the disabled Stripe support button with the MotsDits Stripe Payment Link.
- Replace image placeholders with final commissioned/exported artwork.
